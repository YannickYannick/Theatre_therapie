# Journal projet — Nouveau site théâtre-thérapie Paris

Format BMAD : une ligne par action notable.

- [2026-05-02] Mise en place de l’arborescence `src/`, `public/`, `content/`, `docs/bmad/` (reprise cible depuis `theatre-therapy-paris`, stack TanStack Start + Vite).
- [2026-05-02] Bootstrap technique : backend **Django 5 + DRF** (`backend/`), frontend **Vite + React 19**, BDD **SQLite par défaut** (Postgres/Supabase si `DATABASE_URL`), client Supabase dans `src/integrations/supabase/`, fichiers `.env.example` racine + `backend/`.
- [2026-05-02] README + dépôt GitHub `Theatre_therapie`, doc SQLite par défaut.
